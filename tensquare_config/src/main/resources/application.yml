server:
  port: 12000
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/heitaoc/tensquare_dev.git
  application:
    name: tensquare-config
  rabbitmq:
    host: 192.168.247.128
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh